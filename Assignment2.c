# include <stdio.h>
void main()
{
    int x,y;
    printf("Enter the 2 no.s : ");
    scanf("%d %d",&x,&y);
    if (x>y)
    {
        printf("%d is maximum",x);
    }
    else
    {
        printf("%d is maximum",y);
    }

}